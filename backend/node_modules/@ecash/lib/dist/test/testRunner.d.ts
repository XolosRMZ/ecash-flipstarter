/// <reference types="node" />
import type { ChronikClient } from 'chronik-client';
import type { ChildProcess } from 'node:child_process';
import { Ecc } from '../ecc.js';
import { Script } from '../script.js';
import { OutPoint } from '../tx.js';
export declare class TestRunner {
    ecc: Ecc;
    runner: ChildProcess;
    chronik: ChronikClient;
    private coinsTxid;
    private coinValue;
    private lastUsedOutIdx;
    private constructor();
    static setup(setupScript?: string): Promise<TestRunner>;
    setupCoins(numCoins: number, coinValue: number): Promise<void>;
    getOutpoint(): OutPoint;
    sendToScript(value: number | number[], script: Script): Promise<string>;
    generate(): void;
    stop(): void;
}
//# sourceMappingURL=testRunner.d.ts.map